# Claude

Unity 에디터에서 Claude Code CLI 실행 + git worktree 자동 관리.

## 기능
- `Tools > Claude Code > Open` — 첫 실행: 메인 인스턴스, 이후: git worktree + 새 탭
- `Tools > Claude Code > Settings` — 터미널 색상, 윈도우 이름, 추가 인자 설정
- Windows Terminal 탭 색상 지원 (미설치 시 PowerShell fallback)

## 상태
v0.1.0
